{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window gets Tauri's core defaults and nothing else: no file system, shell, HTTP, or updater access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
